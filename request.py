import xml.etree.ElementTree as ET
import urllib.request
 

response = urllib.request.urlopen('http://52./')
html = response.read()
root = ET.fromstring(html)
if root[0].text=="a"
   
if root[0].text=="b"

if root[0].text=="c"

if root[0].text=="d"

if root[0].text=="e"

if root[0].text=="f"
